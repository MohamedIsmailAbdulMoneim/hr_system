import { NEW_APPRAISAL } from "../actions/ActionTypes";
const initialState = {
    msg: null,
    result: null
};

export default function (state = initialState, action) {
    switch (action.type) {

        default:
            return state

    }

}
